services:
  mos-bot:
    image: ${GHCR_IMAGE:-ghcr.io/owner/repo:latest}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_PATH: ${DATABASE_PATH:-/data/bot.sqlite}
      NODE_ENV: ${NODE_ENV:-production}
    volumes:
      - bot-data:/data

volumes:
  bot-data:
